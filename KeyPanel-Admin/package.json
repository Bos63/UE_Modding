{
  "name": "uassetgui-key-panel",
  "private": true,
  "version": "2.0.1",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "npm run dev -w server",
    "dev:web": "npm run dev -w web",
    "dev:all": "sh -c 'npm run dev -w server & npm run dev -w web & wait'",
    "build": "npm run build -w web",
    "start": "npm run start -w server"
  }
}
